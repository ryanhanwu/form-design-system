
```js
import HideAction from './actions/HideAction';
import DeleteAction from './actions/DeleteAction';

<ActionBar
  selectedCount={666}
  actionHide={<HideAction onClick={() => {}} disabled />}
  actionDelete={<DeleteAction onClick={() => {}} />}
/>
```
